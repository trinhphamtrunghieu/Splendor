name: Test and deploy to GitHub Pages

# Tests run for every push and pull request. Publishing happens only from the
# repository's default branch — whatever it is named — or on a manual run, so
# the workflow needs no editing if the default branch is renamed.
on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Run the rules test suite
        run: node tests/engine.test.js

      # Online play is tested against a real MQTT broker started in-process.
      # These are the only dependencies in the project, and they are dev-only:
      # the published site still loads plain scripts with no build step.
      - name: Install test-only dependencies
        run: npm install --no-audit --no-fund
      - name: Run the online-play test suite
        run: node tests/net.test.mjs

  deploy:
    needs: test
    if: >-
      github.event_name == 'workflow_dispatch' ||
      (github.event_name == 'push' &&
       github.ref_name == github.event.repository.default_branch)
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
        # Turns Pages on (with GitHub Actions as the source) the first time this
        # runs, so no manual repository setting is needed.
        with:
          enablement: true
      - name: Upload the site
        uses: actions/upload-pages-artifact@v3
        with:
          path: .
      - id: deployment
        uses: actions/deploy-pages@v4
